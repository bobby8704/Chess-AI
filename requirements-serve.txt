# Serving-only dependencies for the web app.
#
# This is what a deployment needs — NOT requirements.txt, which additionally pulls in
# torch (~433MB) for training and model export. Inference runs from the committed
# ONNX exports through onnxruntime, and nothing on the request path imports torch
# (proved by bench/test_deploy.py, case D).
#
#   pip install -r requirements-serve.txt
#   python serve.py
#
# Verify the deployment can actually serve before shipping:
#   python bench/test_deploy.py
#
# Pinned EXACTLY to the versions the engine is developed and measured against —
# every strength and latency number in bench/results/ was recorded on these. A
# floating >= here means a deploy can silently run a dependency stack no
# measurement has ever seen.

chess==1.11.2
numpy==2.3.2
onnxruntime==1.28.0
flask==3.1.3

# Production WSGI server (see serve.py) — do not serve with Flask's dev server.
waitress==3.0.2
